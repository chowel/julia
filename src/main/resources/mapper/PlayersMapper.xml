<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.julia.mapper.PlayersMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.julia.entity.PlayersEntity">
        <id column="play_id" property="playId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="secure" property="secure" />
        <result column="web_url" property="webUrl" />
        <result column="web_ip" property="webIp" />
        <result column="login_name" property="loginName" />
        <result column="password" property="password" />
        <result column="verify" property="verify" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        create_time,
        update_time,
        play_id, secure, web_url, web_ip, login_name, password, verify
    </sql>

</mapper>
